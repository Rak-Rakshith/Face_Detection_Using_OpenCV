# Face_Detection_Using_OpenCV
Real-time face detection using Python and OpenCV. The application captures live webcam video, converts frames to grayscale, and uses a Haar Cascade Classifier to detect frontal human faces. Detected faces are highlighted with bounding boxes in real time.
